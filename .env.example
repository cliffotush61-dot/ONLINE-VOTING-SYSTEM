DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=ONLINE_VOTING_SYSTEM
DB_USER=root
DB_PASS=
APP_TIMEZONE=Africa/Nairobi
APP_ENV=production
EVOTE_HASH_SECRET=replace_with_a_long_random_secret

# Optional: use this instead of the DB_* fields above if your host provides one.
# DATABASE_URL=mysql://DB_USER:DB_PASSWORD@DB_HOST:3306/DB_NAME
